# frozen_string_literal: true

require 'instruction'

class InstructionStack
  attr_reader :instruction_arr, :last_addr, :return_addr

  def initialize(addresses)
    # @instruction_arr = Instruction.new(70 % addresses)
    # @last_addr
    # @return_addr
  end
end
